package xyz.rose.gateway.registry

/**
 * Base interface for items that can be registered in a registry.
 * This is a marker interface that all registry items must implement.
 */
interface RegistryItem
