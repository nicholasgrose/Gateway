package com.rose.gateway.shared.trie

class Trie(vararg initialValues: String) {
    init {
        for (value in initialValues) {
            addValue(value)
        }
    }

    private fun addValue(value: String) {
        TODO("Not yet implemented")
    }
}
