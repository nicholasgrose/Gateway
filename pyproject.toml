[project]
name = "gateway"
version = "0.1.0"
description = "Gateway documentation"
dependencies = [
    # https://github.com/zensical/zensical
    "zensical>=0.0.59",
]
requires-python = ">= 3.14"

[tool.uv]
managed = true
